version: '3.8'

services:
  # Go Proxy Service (TEMPORARILY DISABLED)
  # goproxy:
  #   build:
  #     context: ./goproxy
  #     dockerfile: Dockerfile
  #   ports:
  #     - "8003:8003"
  #   environment:
  #     - MONGODB_URI=${MONGODB_URI}
  #     - FACTORY_API_KEY=${FACTORY_API_KEY}
  #     - PROXY_API_KEY=${PROXY_API_KEY}
  #     - DEBUG=${DEBUG:-false}
  #   restart: unless-stopped
  #   depends_on:
  #     - backend

  # Node.js Backend Service
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - ADMIN_SECRET_KEY=${ADMIN_SECRET_KEY}
      - BACKEND_PORT=3000
    restart: unless-stopped

networks:
  default:
    name: fproxy-network
