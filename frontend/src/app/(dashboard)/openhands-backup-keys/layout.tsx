export const metadata = {
  title: 'OpenHands Backup Keys | TrollLLM',
  description: 'Manage backup API keys for OpenHands auto rotation',
}

export default function OpenHandsBackupKeysLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
