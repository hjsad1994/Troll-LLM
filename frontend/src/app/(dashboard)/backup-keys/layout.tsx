export const metadata = {
  title: 'Backup Keys | TrollLLM',
  description: 'Manage backup API keys for auto rotation',
}

export default function BackupKeysLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
